# 📱 Telegram Бот для eSIM

Этот проект — Telegram-бот с мини-приложением и REST-фронендом для продажи eSIM. Пользователь может выбрать тариф, оплатить, получить QR-код, а также управлять своими eSIM (просмотр, пополнение, удаление, отмена).

---

## 🔧 Основные компоненты

### 1. **bot.py** — Telegram-бот

Бот написан на `python-telegram-bot` и обрабатывает команды, inline-кнопки, текстовые сообщения и сессии пользователя. Он взаимодействует с backend-API и модулем `buy_esim.py` для покупки и управления eSIM.

#### Главное меню (`main_menu_keyboard`):

* 🖥️ Open Mini App — открывает WebApp
* 🛒 Buy eSIM — выбор тарифа: локальный / региональный / глобальный
* 🔑 My eSIMs — список eSIM пользователя
* ❓ FAQ — ответы на частые вопросы
* 📌 Guides — гайды по настройке
* 🆕 Project News — канал новостей
* 💬 Support — чат поддержки

#### Основные обработчики:

* `start(update, context)` — приветствие и сохранение пользователя в БД
* `handle_message(update, context)` — обработка текстовых сообщений:

  * выбор тарифа
  * оплата криптой или картой
  * запрос страны
  * отображение кнопок оплаты
* `button_handler(update, context)` — обработка inline-кнопок:

  * выбор страны / региона / глобального пакета
  * отображение таблицы с тарифами
  * кнопки "More Info", "Buy", "Top-up", "Cancel", "Refresh", "Delete"

#### Состояния:

Используются `context.chat_data` для отслеживания временных состояний:

* `pending_purchase` — ожидается ввод количества eSIM
* `awaiting_country_search` — поиск страны по названию
* `awaiting_payment` — ожидается подтверждение оплаты
* `current_table_text` и `current_button_rows` — сохранение разметки для пагинации

#### Обработка eSIM:

* `🔑 My eSIMs` вызывает метод `buy_esim.my_esim(user_id)`
* Каждая eSIM отображается с:

  * названием пакета, объемом, сроком, usage
  * статусом (New, In Use, Deleted, и т.д.)
  * QR-кодом для скачивания
  * кнопками "Cancel", "Top-up", "Delete", "Refresh Usage"

#### Помощники:

* `country_code_to_emoji()` — флаг по коду страны
* `get_esim_status_label()` — определение состояния eSIM
* `format_esim_info()` — форматирование текста eSIM для отображения в Telegram

---

### 2. **buy\_esim.py** — логика работы с ESIMAccess API

Асинхронная работа с API провайдера eSIM:

* `check_balance()` — проверить баланс аккаунта
* `place_order(...)` — сделать заказ eSIM
* `poll_profile(...)` — получить QR-профили после заказа
* `process_purchase(...)` — полный цикл покупки eSIM
* `query_profile(order_no)` — получить eSIM по номеру заказа
* `query_esim_by_iccid(iccid)` — получить статус eSIM по ICCID
* `cancel_esim(...)` — отменить eSIM
* `get_topup_packages(iccid)` — доступные пополнения для eSIM
* `topup_esim(...)` — пополнение eSIM
* `my_esim(user_id)` — все eSIM пользователя
* `update_usage_by_iccid(...)` — обновление статистики использования
* `query_usage(tran_no)` — получение usage по транзакции

> Все запросы делаются через `aiohttp`, с ретраями и логгированием

---

### 3. **auth.py** — авторизация WebApp (мини-приложение)

* `verify_telegram_auth(...)` — верификация initData по токену бота
* `auth_telegram()` — точка входа POST /auth/telegram, возвращает user\_id

---

### 4. **esim\_routes.py** — FastAPI маршруты (бэкенд)

* `GET /my-esims` — список eSIM пользователя
* `POST /buy` — купить eSIM
* `POST /cancel` — отмена eSIM
* `POST /topup` — пополнение eSIM
* `GET /topup-packages` — получить доступные пополнения
* `POST /refresh` — обновить usage по eSIM
* `POST /delete` — удалить eSIM из БД

> Все маршруты работают с заголовком `X-User-ID`

---

### 5. **database.py** — инициализация базы данных

* `SessionLocal` — сессия SQLAlchemy
* `upsert_user(db, user_data)` — создать или обновить пользователя
* `Base.metadata.create_all(...)` — создание таблиц

---

### 6. **models.py** — SQLAlchemy модели

#### User

* telegram\_id
* username
* photo\_url
* last\_login

#### Order

* user\_id, order\_id, transaction\_id, iccid
* package\_code, count, period\_num
* price, retail\_price, qr\_code
* status, esim\_status, smdp\_status
* expired\_time, total\_volume, total\_duration, order\_usage
* esim\_list, package\_list
* created\_at, updated\_at, last\_update\_time

---

## 🔐 Безопасность

* Авторизация в WebApp проверяется через `initData` (Telegram WebApp auth)
* Покупки разрешены только whitelisted-пользователю (на момент тестов)
* eSIM операции логируются, используются retry и timeout